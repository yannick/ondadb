# bca baseline file. Generated by `bca check --write-baseline`.
# Listed offenders are filtered from threshold checks; a function that
# gets worse than its recorded value still fails. Refresh with
# `--write-baseline` when entries become stale.
#
# Path keys are relative to this file's directory (the anchor), so the
# baseline survives any `--paths` form (`.`, `src/`, `$PWD`, …).
version = 6

[provenance]
tier = "hard"

[[entry]]
path = "src/compaction.rs"
qualified = "compact_inputs"
metric = "cognitive"
value = 107.0

[[entry]]
path = "src/compaction.rs"
qualified = "compact_inputs"
metric = "cyclomatic"
value = 46.0

[[entry]]
path = "src/error.rs"
qualified = "OndaError::code"
metric = "cyclomatic"
value = 16.0

[[entry]]
path = "src/error.rs"
qualified = "OndaError::fmt"
metric = "cyclomatic"
value = 16.0

[[entry]]
path = "src/error.rs"
qualified = "OndaError::kind"
metric = "cyclomatic"
value = 16.0
